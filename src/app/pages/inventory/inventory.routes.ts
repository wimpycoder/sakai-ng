import { Routes } from '@angular/router';
import { InventoryDashboard } from './inventory-dashboard.component';

export const inventoryRoutes: Routes = [
    {
        path: '',
        component: InventoryDashboard
    }
];
