import { Routes } from '@angular/router';
import { SalesDashboard } from './sales-dashboard.component';

export const salesRoutes: Routes = [
    {
        path: '',
        component: SalesDashboard
    }
];
